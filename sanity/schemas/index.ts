import { processType, stepType, feeType, localizedTitleType } from './process'
import { guideType, costType } from './guide'
import { alertType } from './alert'

export const schemaTypes = [processType, stepType, feeType, localizedTitleType, guideType, costType, alertType]
